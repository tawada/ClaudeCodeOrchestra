/**
 * »Ã·çóâ¸åüën¨óÈêüİ¤óÈ
 */

const controllers = require('./controllers');
const models = require('./models');
const routes = require('./routes');

module.exports = {
  controllers,
  models,
  routes
};